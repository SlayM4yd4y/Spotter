@page "/"
@using Spotter.Client.Services
@inject ApiService ApiService

<h3>Flask API Data</h3>
<p>@message</p>

@code {
    private string message;

    protected override async Task OnInitializedAsync()
    {
        message = await ApiService.GetApiData();
    }
}
